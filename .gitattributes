# ------------------------
# Normalize line endings for text files (force LF across platforms)
# ------------------------
* text=auto
*.py text eol=lf
*.md text eol=lf
*.rst text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
*.toml text eol=lf
*.csv text eol=lf
*.sh text eol=lf
*.ps1 text eol=crlf

# ------------------------
# Explicit binary types (no CRLF conversion, no diffs)
# ------------------------
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
*.tgz binary
*.rar binary
*.7z binary
*.mp4 binary
*.mov binary
*.wav binary

# ------------------------
# Model files & large artifacts — recommend tracking via Git LFS
# ------------------------
# These attributes help ensure consistent handling and recommend LFS usage.
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.ckpt filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
*.bin filter=lfs diff=lfs merge=lfs -text
weights/** filter=lfs diff=lfs merge=lfs -text
model_weights/** filter=lfs diff=lfs merge=lfs -text
checkpoints/** filter=lfs diff=lfs merge=lfs -text

# Large datasets
data/** filter=lfs diff=lfs merge=lfs -text
*.parquet filter=lfs diff=lfs merge=lfs -text

# Legal document bundles and generated PDFs should be treated as large artifacts
legal_docs/** filter=lfs diff=lfs merge=lfs -text
docs/**/*.pdf filter=lfs diff=lfs merge=lfs -text

# ------------------------
# Disable diffs for binary files to avoid noise
# ------------------------
*.pdf -diff
*.png -diff
*.jpg -diff
*.zip -diff
*.bin -diff

# ------------------------
# Export / archive hygiene
# ------------------------
*.log export-ignore
.env export-ignore
.env.* export-ignore
*.pyc export-ignore

# ------------------------
# Repository metadata and language tagging
# ------------------------
# Mark vendored/third-party folders so GitHub linguist treats them appropriately
vendor/** linguist-vendored=true

# Exclude generated docs and large bundles from archives
docs/** export-ignore
legal_docs/** export-ignore

# Guidance: recommend running `git lfs install` and tracking large artifacts
# e.g. `git lfs track "weights/*.pt"`
